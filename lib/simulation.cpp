#include "simulation.hpp"

int simulation(const Parcours& parcours, const std::vector<Coureur>& coureurs) {
    /*************************************************
       Use threads to display the different modules
    *************************************************/
    /* MODULES:
    *       - Top Coureurs Display (top left)
    *       - Live Parcours (and top 5 coureurs ?) (middle left)
    *       - Stopwatch (top right)
    *       - Top Speeds Display (bottom right)
    */

    // INITIALISATION
    
    
    

    return 0;
}
